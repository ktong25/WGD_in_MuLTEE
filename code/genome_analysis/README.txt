1- Take raw genome reads and calculate coverage data (which are then used to calculate chromosome copy number), and at the same time, call mutations in the genomes.

2- Calculate the allele frequency for each de novo mutation, after converting a VCF to a TAB file.
